import * as Styled from './Styled';
import TodoContainer from './TodoContainer';

function TodoMain() {
  return (
    <Styled.TodoMain>
      <TodoContainer />
    </Styled.TodoMain>
  );
}

export default TodoMain;
