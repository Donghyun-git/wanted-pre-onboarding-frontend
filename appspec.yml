version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/deploy
    overwrite: yes
permissions:
  - object: /home/ubuntu/deploy
    owner: root
    group: root
    mode: 755
hooks:
  AfterInstall:
    - location: /home/ubuntu/deploy/deploy.sh
      timeout: 60
      runas: root
